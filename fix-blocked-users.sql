-- =============================================================================
-- СКРИПТ ДЛЯ РАЗБЛОКИРОВКИ ПОЛЬЗОВАТЕЛЕЙ
-- =============================================================================
-- Используйте этот скрипт, если все пользователи оказались заблокированы
-- после обновления кода
-- =============================================================================

-- Разблокировать всех пользователей
UPDATE users SET is_blocked = false WHERE is_blocked = true;

-- Проверить статус пользователей
SELECT id, email, role, is_blocked, created_at 
FROM users 
ORDER BY created_at DESC;

-- Разблокировать конкретного пользователя по email
-- UPDATE users SET is_blocked = false WHERE email = 'your-email@example.com';

-- Разблокировать только администраторов
-- UPDATE users SET is_blocked = false WHERE role IN ('admin', 'superadmin') AND is_blocked = true;

-- =============================================================================
-- ПРИМЕЧАНИЯ
-- =============================================================================
-- 1. Выполните этот скрипт в вашей PostgreSQL базе данных
-- 2. После выполнения перезапустите сервер
-- 3. Проверьте, что пользователи могут войти
-- =============================================================================

