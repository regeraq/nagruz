# ✅ ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ

**Дата деплоя:** 2026-01-31  
**Статус:** ✅ Деплой выполнен успешно

---

## 📊 РЕЗУЛЬТАТЫ ДЕПЛОЯ

### ✅ Выполнено:

1. **Миграция БД:**
   ```
   [✓] Pulling schema from database...
   [i] No changes detected
   ```
   ✅ Схема БД актуальна (таблица `commercial_proposal_files` существует или не требуется)

2. **Сборка проекта:**
   ```
   ✓ built in 15.97s
   dist/index.js  270.5kb
   ```
   ✅ Проект успешно собран

3. **Перезапуск приложения:**
   ```
   [PM2] [loaddevice](0) ✓
   status: online
   ```
   ✅ Приложение запущено и работает

---

## 🔍 ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### 1. Проверка таблицы БД (если нужно)

Выполните в psql или через админ-панель:
```sql
-- Проверить существование таблицы
SELECT EXISTS (
  SELECT FROM information_schema.tables 
  WHERE table_schema = 'public' 
  AND table_name = 'commercial_proposal_files'
);

-- Проверить структуру таблицы
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'commercial_proposal_files'
ORDER BY ordinal_position;
```

### 2. Проверка логов приложения

```bash
# Просмотр логов PM2
pm2 logs loaddevice --lines 50

# Проверка на ошибки
pm2 logs loaddevice --err --lines 100
```

**Что искать:**
- ✅ Нет ошибок подключения к БД
- ✅ Нет ошибок при старте сервера
- ✅ Нет ошибок "table does not exist"

### 3. Функциональное тестирование

#### A. Загрузка файлов (форма коммерческого предложения)

1. Откройте главную страницу сайта
2. Перейдите к разделу "Контакты" (форма коммерческого предложения)
3. Заполните форму
4. Попробуйте загрузить:
   - ✅ Один файл (PDF, DOC, DOCX, XLS, XLSX)
   - ✅ Несколько файлов одновременно
   - ✅ Файл больше 10 МБ (должна быть ошибка)
   - ✅ Неподдерживаемый формат (должна быть ошибка)

**Ожидаемый результат:**
- ✅ Файлы загружаются успешно
- ✅ Валидация работает корректно
- ✅ Ошибки отображаются понятно

#### B. Мониторинг БД (админ-панель)

1. Войдите в админ-панель
2. Перейдите на вкладку "База данных"
3. Проверьте:
   - ✅ Отображается общий размер БД
   - ✅ Отображается размер таблиц
   - ✅ Отображается размер индексов
   - ✅ Таблица с детальной информацией по таблицам
   - ✅ Кнопка "Обновить" работает

**Ожидаемый результат:**
- ✅ Все метрики отображаются
- ✅ Данные обновляются
- ✅ Нет ошибок загрузки

#### C. Просмотр заявок (админ-панель)

1. В админ-панели перейдите к заявкам с сайта
2. Проверьте:
   - ✅ Отображается количество файлов для каждой заявки
   - ✅ Кнопка "Просмотр" открывает модальное окно
   - ✅ В модальном окне отображаются прикрепленные файлы
   - ✅ Кнопки скачивания файлов работают

**Ожидаемый результат:**
- ✅ Файлы отображаются корректно
- ✅ Скачивание файлов работает
- ✅ Нет ошибок при загрузке данных

#### D. Профиль пользователя

1. Войдите как обычный пользователь
2. Перейдите в профиль
3. Откройте вкладку "Коммерческие предложения"
4. Проверьте:
   - ✅ Отображаются все предложения пользователя
   - ✅ Для каждого предложения отображаются файлы
   - ✅ Кнопки скачивания файлов работают

**Ожидаемый результат:**
- ✅ Данные загружаются корректно
- ✅ Файлы доступны для скачивания
- ✅ Нет ошибок доступа

---

## 🐛 ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: Таблица не существует

**Симптомы:**
- Ошибка в логах: "Table 'commercial_proposal_files' does not exist"
- Файлы не сохраняются при отправке формы

**Решение:**
```bash
# Применить миграцию
npm run db:push

# Или создать таблицу вручную
psql -U loaddevice_user -d loaddevice_db -f create_commercial_proposal_files.sql
```

### Проблема 2: Ошибки при загрузке файлов

**Симптомы:**
- Ошибка 500 при отправке формы
- Файлы не загружаются

**Решение:**
1. Проверить логи: `pm2 logs loaddevice --err`
2. Проверить размер файлов (максимум 10 МБ на файл, 50 МБ общий)
3. Проверить формат файлов (только PDF, DOC, DOCX, XLS, XLSX)

### Проблема 3: Мониторинг БД не работает

**Симптомы:**
- Ошибка загрузки данных в админ-панели
- Не отображается информация о размере БД

**Решение:**
1. Проверить права доступа пользователя БД
2. Проверить логи: `pm2 logs loaddevice --err`
3. Проверить, что endpoint `/api/admin/database/size` доступен

### Проблема 4: Приложение не запускается

**Симптомы:**
- PM2 показывает статус "errored" или "stopped"
- Ошибки в логах

**Решение:**
```bash
# Проверить статус
pm2 status

# Просмотреть логи
pm2 logs loaddevice --err --lines 100

# Перезапустить
pm2 restart loaddevice

# Если не помогает, пересобрать
npm run build
pm2 restart loaddevice
```

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ

- [ ] Логи приложения не содержат критических ошибок
- [ ] Форма загрузки файлов работает
- [ ] Валидация файлов работает (размер, тип)
- [ ] Загрузка нескольких файлов работает
- [ ] Мониторинг БД отображается в админ-панели
- [ ] Заявки с файлами отображаются в админ-панели
- [ ] Скачивание файлов работает
- [ ] Профиль пользователя отображает предложения с файлами
- [ ] Старые функции работают (обратная совместимость)

---

## 📊 МОНИТОРИНГ

### Рекомендуется отслеживать:

1. **Размер БД:**
   - Проверять регулярно через админ-панель
   - Настроить алерт при превышении 80% лимита

2. **Количество файлов:**
   - Мониторить рост количества файлов
   - При необходимости оптимизировать хранение

3. **Производительность:**
   - Отслеживать время ответа API
   - Мониторить использование памяти

4. **Ошибки:**
   - Регулярно проверять логи
   - Настроить алерты на критические ошибки

---

## 🎉 УСПЕШНЫЙ ДЕПЛОЙ

Если все проверки пройдены успешно:

✅ **Деплой завершен успешно!**

Все новые функции работают:
- ✅ Множественная загрузка файлов
- ✅ Мониторинг размера БД
- ✅ Улучшенная безопасность

**Следующие шаги:**
1. Продолжить мониторинг в течение первых 24 часов
2. Собрать обратную связь от пользователей
3. Планировать дальнейшие улучшения

---

**Дата проверки:** _______________  
**Проверил:** _______________  
**Статус:** _______________

