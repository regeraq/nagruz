# 🚀 Инструкция по Запуску Сервера

## Предварительные требования

Для запуска сайта необходимо установить:

1. **Node.js** (версия 18 или выше)
   - Скачайте с [nodejs.org](https://nodejs.org/)
   - Или установите через пакетный менеджер (chocolatey, winget)

2. **npm** (устанавливается вместе с Node.js)

## Шаги для запуска

### 1. Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
cd "C:\Users\Andrey\Downloads\HelloWhoAreYou-1 (1)\HelloWhoAreYou-1"
npm install
```

### 2. Запуск сервера разработки

После установки зависимостей запустите:

**Для Windows PowerShell:**
```powershell
$env:NODE_ENV="development"
npm run dev
```

**Для Windows CMD:**
```cmd
set NODE_ENV=development
npm run dev
```

**Для Linux/Mac:**
```bash
NODE_ENV=development npm run dev
```

### 3. Открытие в браузере

После запуска сервер будет доступен по адресу:
- **http://localhost:5000** (или порт, указанный в переменной окружения PORT)

## Проверка установки Node.js

Выполните в терминале:
```bash
node --version
npm --version
```

Если команды не работают, установите Node.js с официального сайта.

## Возможные проблемы

### Проблема: "npm не является внутренней или внешней командой"
**Решение:** Установите Node.js с [nodejs.org](https://nodejs.org/)

### Проблема: Ошибки при установке зависимостей
**Решение:** 
```bash
npm cache clean --force
npm install
```

### Проблема: Порт уже занят
**Решение:** Измените порт в переменной окружения:
```powershell
$env:PORT=3000
npm run dev
```

## Что было улучшено

После запуска вы увидите следующие улучшения:

✅ **Безопасность:**
- Rate limiting на всех API эндпоинтах
- Защита от XSS атак
- Security headers
- Валидация и санитизация входных данных

✅ **Производительность:**
- Кэширование курсов криптовалют (30 сек)
- Кэширование товаров (5 мин)
- Таймауты для внешних API

✅ **Надежность:**
- Улучшенная обработка ошибок
- Исправлены утечки памяти
- Асинхронная отправка email

## Проверка работы улучшений

1. **Rate Limiting:** Попробуйте отправить форму контакта более 5 раз за минуту - должен вернуться статус 429
2. **Кэширование:** Запросите `/api/crypto-rates` дважды - второй запрос будет мгновенным
3. **XSS защита:** Попробуйте отправить HTML в форме - он будет экранирован

## Остановка сервера

Нажмите `Ctrl+C` в терминале, где запущен сервер.


