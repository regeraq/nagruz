# 🔄 ОБНОВЛЕНИЕ ПРОЕКТА НА СЕРВЕРЕ

## 📋 Данные сервера:
- **IP:** 45.9.72.103
- **Домен:** vm3848909.firstbyte.club
- **Пользователь:** root
- **Директория проекта:** /var/www/loaddevice

---

## 🚀 Способ 1: Через SSH (РЕКОМЕНДУЕТСЯ)

### Подключитесь к серверу:

```bash
ssh root@45.9.72.103
```

### Выполните команды обновления:

```bash
# Перейдите в директорию проекта
cd /var/www/loaddevice

# Получите последние изменения с GitHub
git fetch origin
git reset --hard origin/main

# Проверьте структуру проекта
ls -la

# Если проект в подпапке HelloWhoAreYou-1, перейдите туда
cd HelloWhoAreYou-1 2>/dev/null || true

# Установите зависимости (если нужно)
npm install

# Пересоберите проект
npm run build

# Примените миграции БД (если есть изменения)
npm run db:push

# Перезапустите приложение
pm2 restart loaddevice

# Проверьте статус
pm2 status
pm2 logs loaddevice --lines 20
```

---

## 🚀 Способ 2: Одной командой (быстро)

```bash
ssh root@45.9.72.103 "cd /var/www/loaddevice && git fetch origin && git reset --hard origin/main && cd HelloWhoAreYou-1 2>/dev/null || true && npm install && npm run build && npm run db:push && pm2 restart loaddevice && pm2 logs loaddevice --lines 20"
```

---

## 🚀 Способ 3: Через PowerShell (Windows)

Откройте PowerShell и выполните:

```powershell
# Подключение к серверу и обновление
ssh root@45.9.72.103 @"
cd /var/www/loaddevice
git fetch origin
git reset --hard origin/main
cd HelloWhoAreYou-1 2>/dev/null || true
npm install
npm run build
npm run db:push
pm2 restart loaddevice
pm2 logs loaddevice --lines 20
"@
```

---

## 🔍 Проверка после обновления

### 1. Проверьте статус PM2:
```bash
pm2 status
```

### 2. Проверьте логи:
```bash
pm2 logs loaddevice --lines 50
```

### 3. Проверьте работу сайта:
- Откройте в браузере: http://45.9.72.103
- Или: https://vm3848909.firstbyte.club

---

## ⚠️ Если возникли проблемы:

### Проблема: "package.json не найден"
**Решение:** Проверьте структуру проекта:
```bash
cd /var/www/loaddevice
ls -la
find . -name "package.json" -type f
```

### Проблема: "Git pull не работает"
**Решение:** Проверьте подключение к GitHub:
```bash
cd /var/www/loaddevice
git remote -v
git fetch origin
```

### Проблема: "PM2 процесс не запускается"
**Решение:** Проверьте логи и перезапустите:
```bash
pm2 logs loaddevice --lines 50
pm2 delete loaddevice
cd /var/www/loaddevice
cd HelloWhoAreYou-1 2>/dev/null || true
pm2 start dist/index.js --name loaddevice --max-memory-restart 500M
pm2 save
```

---

## 📝 Примечания:

- ✅ Все команды выполняются от имени пользователя `root`
- ✅ Проект находится в `/var/www/loaddevice`
- ✅ Если структура изменилась, возможно нужно перейти в подпапку `HelloWhoAreYou-1`
- ✅ После обновления проверьте работу сайта в браузере
