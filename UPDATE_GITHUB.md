# 🔄 ОБНОВЛЕНИЕ ПРОЕКТА НА GITHUB

## Быстрый способ:

### Вариант 1: Использовать PowerShell скрипт

1. Откройте PowerShell в папке проекта
2. Выполните:
   ```powershell
   .\update-github.ps1
   ```

### Вариант 2: Выполнить команды вручную

Откройте PowerShell в папке проекта и выполните:

```powershell
# Перейдите в директорию проекта
cd "C:\Users\k62\Documents\Атом\сайт\HelloWhoAreYou-1 (5)\HelloWhoAreYou-1"

# Проверьте статус
git status

# Добавьте изменения
git add -A

# Создайте коммит
git commit -m "Update: Site improvements and fixes"

# Загрузите на GitHub
git push origin main
```

## После обновления на GitHub:

Обновите код на сервере:

```bash
ssh root@45.9.72.103
cd /var/www/loaddevice
git pull origin main
npm install
npm run build
pm2 restart loaddevice
```

Или если настроен автоматический деплой - изменения применятся автоматически через 1-2 минуты!

